<div class="row">
    <div class="col-md-6">
      <% if @product.images.present? %>
      <%= image_tag(@product.images[1].medium.url, class:"thumbnail") %>
      <% else %>
      <%= image_tag("http://placehold.it/400x400?text=商品无图", class:"thumbnail") %>
      <% end %>
      </div>
      <div class="col-md-6">
        <h2><%= @product.title %></h2>
        <div style="height:100px;">
          <p>
            <%= @product.description %>
          </p>
        </div>



          数量:<%= @product.quantity %>


        <div class="product-price"> ￥ <%= @product.price %> </div>
          <div class="pull-right">
          <%= link_to("加入购物车", "#", :class => "btn btn-primary btn-lg btn-danger") %>
            </div>

          </div>
        </div>

 
